import { BarChart } from '@tremor/react'
import { PageHeader } from '../../components/PageHeader'
import { membrosIniciais } from '../../data/membros'
import { formatarData } from '../../utils/formatters'

export default function BatismosPage(){const batizados=membrosIniciais.filter(m=>m.batizado), candidatos=membrosIniciais.filter(m=>!m.batizado);return <><PageHeader titulo="Batismos" subtitulo="Acompanhamento dos batizados e candidatos"/><div className="tab-panels"><section className="members-panel"><div className="panel-heading"><div><h2>Já batizados</h2><p>Histórico de batismos registrados</p></div></div><div className="table-wrap"><table><thead><tr><th>Membro</th><th>Data</th><th>Igreja</th><th>Pastor</th></tr></thead><tbody>{batizados.map(m=><tr key={m.id}><td>{m.nome}</td><td>{formatarData(m.dataBatismo)}</td><td>{m.igrejaBatismo}</td><td>{m.pastorBatismo}</td></tr>)}</tbody></table></div></section><section className="members-panel"><div className="panel-heading"><div><h2>Candidatos ao batismo</h2><p>Pessoas aguardando acompanhamento</p></div></div><div className="candidate-list">{candidatos.map(m=><article key={m.id}><div className="member"><b>{m.nome.slice(0,2)}</b><div><strong>{m.nome}</strong><span>{m.telefone}</span></div></div><span className="badge pendente">Curso pendente</span></article>)}</div></section><section className="analytics-card baptism-history"><h2>Histórico anual</h2><BarChart className="tremor-chart tall" data={[28,36,31,45,52,61].map((valor,i)=>({ano:String(2021+i),Batismos:valor}))} index="ano" categories={['Batismos']} colors={['emerald']} showLegend={false}/></section></div></>}

